/// Spacing constants for the admin table.
/// Change here → applies to every table in the admin panel.
library;

// ── Layout ────────────────────────────────────────────────────────────────────
const double kTableH     = 20.0; // outer horizontal inset (card edge → first col)
const double kColGap     = 24.0; // total gap between adjacent columns (split 12/12)
const double kIdW        = 64.0; // fixed width reserved for the ID column

// ── Sizing ────────────────────────────────────────────────────────────────────
const double kRowH       = 62.0; // data-row height
const double kHeaderH    = 48.0; // header-row height
const double kScrollbarH = 14.0; // sticky horizontal-scrollbar strip height
const double kDividerH   =  1.0; // divider thickness between header and rows

import 'package:flutter/material.dart';
class UserHomePlaceholder extends StatelessWidget {
  const UserHomePlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('User App'),
      ),
    );
  }
}
